[package]
name = "phonton-handoff"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Deterministic HandoffPacket builder for Phonton ADE"

[dependencies]
phonton-types = { path = "../phonton-types" }
serde_json = { workspace = true }
