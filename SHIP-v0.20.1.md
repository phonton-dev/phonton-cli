# Ship v0.20.1

1. Copy `phonton-dev/` to clean clone of `phonton-cli` per `repo-boundaries.md`
2. `cargo test --locked --workspace`
3. `cargo build --release --locked -p phonton-cli`
4. Attach RunIndex 38 report from `C:\benchmark-phonton`
5. `git tag v0.20.1 && git push origin v0.20.1`
